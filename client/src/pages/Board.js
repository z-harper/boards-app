import React from 'react'

// make request to board api with the given id from props

const Board = (props) => {
  console.log(props);
  
  return (
    <div>
      hello from the board page
    </div>
  )
}

export default Board
